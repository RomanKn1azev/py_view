tasks:
  classification:
    required_columns:
      - y_true
      - y_pred
    
    dtypes:
      y_true: int
      y_pred: int

  detect:
    required_columns:
      - y_true
      - y_pred
      - true_box
      - pred_box

    dtypes:
      y_true: int
      y_pred: int
      true_box: list[int]
      pred_box: list[int]